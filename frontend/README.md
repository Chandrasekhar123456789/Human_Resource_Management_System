# HRMS Frontend (Vite + React)

## Quick start
- cd frontend
- npm install
- npm run dev

Default expects backend at http://localhost:5000 (use VITE_API_BASE to change).

Use the backend seed script to populate demo data:
- cd ../backend
- npm run migrate
- npm run seed
